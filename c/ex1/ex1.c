// include standard IO headers.
#include <stdio.h>

// main - program entrypoint.
int main(int argc, char *argv[])
{ 
    // declare and assign integer variable.
    int distance = 100;

    // Print out distance.
    printf("You are %d miles away.\n", distance);

    // Return with non-zero exit code.
    return 0;
}